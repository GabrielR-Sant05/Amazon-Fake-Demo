to activate .venv(its necessary to use the api)
 .venv\Scripts\Activate.ps1

 to create a new virtual ambient
 python -m venv .venv

to execute the project 
uvicorn main:app --reload

if uvicorn dosn't are reconhed to terminal, for resolved is use
pip install uvicorn